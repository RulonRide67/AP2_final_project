version: v2

modules:
  - path: .
    name: buf.build/madiyar/auth-service

deps:
  - buf.build/envoyproxy/protoc-gen-validate

lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_VERSION_SUFFIX

breaking:
  use:
    - FILE
